# Backend image is built from the repo root; keep the context lean and never
# ship local secrets or build artifacts into the image.
**/.env
**/*.env
!**/*.env.example
.git
.gitignore
**/__pycache__/
**/*.pyc
.azure/
node_modules/
README.md
